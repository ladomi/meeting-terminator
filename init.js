/*
 * init.js
 */

 function init() {
  var sample_topics = 'Topic 1\nTopic 2';
  var textarea_topics = document.getElementById('textarea-topics');
  textarea_topics.value = sample_topics
  var sample_duration = '2\n5';
  var textarea_duration = document.getElementById('textarea-duration');
  textarea_duration.value = sample_duration
}

init()
